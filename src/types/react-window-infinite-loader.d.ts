// react-window-infinite-loader@1.x does not ship its own type definitions and
// there is no matching @types package for that major version (the published
// @types stub targets v2). Declare a minimal ambient module so the project
// type-checks. Replace with real types if the dependency is upgraded to v2+.
declare module 'react-window-infinite-loader';
